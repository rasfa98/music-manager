{{>flash}}

<div class="row">
    {{#each rows}}
    <div class="card col-sm-6">
        <div class="card-body">
            <h5 class="card-title">{{albumTitle}}</h5>
            <p class="card-text">{{band}}</p>
        </div>
    </div>
    {{/each}}
</div>